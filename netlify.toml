[build]
  command = "npm run generate"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[dev]
  command = "npm run dev"
publish = "dist"
[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = ""