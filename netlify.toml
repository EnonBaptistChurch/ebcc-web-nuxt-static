[build]
  command = "npm run generate"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[dev]
  command = "npm run dev"
publish = "dist"
[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = ""